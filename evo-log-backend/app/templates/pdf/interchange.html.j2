<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .header { text-align: center; margin-bottom: 40px; }
        .header h1 { color: #1e40af; margin: 0; }
        .header p { color: #6b7280; margin: 5px 0; }
        .info-box { border: 2px solid #1e40af; padding: 20px; margin: 20px 0; border-radius: 8px; }
        .info-row { display: flex; justify-content: space-between; margin: 10px 0; }
        .info-label { font-weight: bold; color: #374151; }
        .info-value { color: #111827; }
        .footer { margin-top: 60px; text-align: center; color: #6b7280; font-size: 12px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>INTERCHANGE</h1>
        <p>EVO-LOG EM-ERP  Port de Douala</p>
        <p>NumÃ©ro du conteneur: {{ stock.numero_conteneur or 'N/A' }}</p>
    </div>

    <div class="info-box">
        <div class="info-row">
            <span class="info-label">Date de sortie:</span>
            <span class="info-value">{% if stock.date_gate_out %}{{ stock.date_gate_out.strftime('%d/%m/%Y %H:%M') }}{% else %}N/A{% endif %}</span>
        </div>
        <div class="info-row">
            <span class="info-label">Plomb:</span>
            <span class="info-value">N/A</span>
        </div>
        <div class="info-row">
            <span class="info-label">Ã‰tat des structures:</span>
            <span class="info-value">N/A</span>
        </div>
    </div>

    <div class="info-box">
        <div class="info-row">
            <span class="info-label">Dossier:</span>
            <span class="info-value">{% if dossier %}{{ dossier.reference }}{% else %}N/A{% endif %}</span>
        </div>
        <div class="info-row">
            <span class="info-label">Marchandise:</span>
            <span class="info-value">{% if marchandise %}{{ marchandise.designation }}{% else %}N/A{% endif %}</span>
        </div>
    </div>

    <div class="footer">
        <p>Document gÃ©nÃ©rÃ© automatiquement par EVO-LOG EM-ERP</p>
        <p>Port Autonome de Douala  Cameroun</p>
        <p>Date d'Ã©mission: {{ datetime_generation }}</p>
    </div>
</body>
</html>
