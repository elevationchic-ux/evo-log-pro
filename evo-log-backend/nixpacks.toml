[phases.setup]
nixPkgs = [
  "python311",
  "cairo",
  "pango",
  "gdk-pixbuf",
  "libffi",
  "openssl",
  "gcc",
  "fontconfig",
  "freetype",
  "harfbuzz",
  "gobject-introspection",
  "glib",
  "zlib"
]

[phases.build]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "cd evo-log-backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1 || uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1"

[variables]
PORT = "8000"
PYTHONUNBUFFERED = "1"
