# Force LF line endings for shell scripts (prevent Windows CRLF corruption)
*.sh text eol=lf

# Force LF for Docker-related files
Dockerfile text eol=lf
*.dockerfile text eol=lf
docker-compose*.yml text eol=lf

# Detect text files automatically
* text=auto
